build:
  docker:
    web: Dockerfile.web
    tensorflow: Dockerfile.tensorflow
run:
  web: sh -c "gunicorn --bind 0.0.0.0:${PORT:-8000} app:app"
  tensorflow: /usr/bin/tensorflow_model_server --model_base_path=/models/model_name --rest_api_port=8501
